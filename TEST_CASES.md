# ShadowKey 核心测试用例

## 录制模块

### TC-001: 键盘录制准确率
- **前置**: 程序运行，不在录制状态
- **操作**: 按下"Start Recording"，依次按下 A、B、C、D 四个键各一次
- **预期**: 录制到 8 个事件（4 个 KeyDown + 4 个 KeyUp），事件顺序正确
- **验证**: UI 事件列表显示 8 条记录，vk_code 依次为 0x41、0x42、0x43、0x44
- **通过标准**: 准确率 100%

### TC-002: 鼠标录制
- **前置**: 程序运行
- **操作**: 开始录制后，在屏幕(500,300)处左键单击一次，在(800,600)处右键单击一次
- **预期**: 捕获到 LeftDown、LeftUp、RightDown、RightUp 事件，坐标正确
- **验证**: 坐标偏差 ≤ 2px

### TC-003: 录制暂停/恢复
- **前置**: 程序运行
- **操作**: 开始录制 → 输入 A → 暂停 → 输入 B → 恢复 → 输入 C → 停止
- **预期**: 只录制到 A 和 C 的按键事件，暂停期间的 B 不被记录
- **通过标准**: 实际录制事件数 = 4（A down/up + C down/up）

## 脚本存取

### TC-004: 脚本序列化/反序列化
- **前置**: 录制了 10 个事件
- **操作**: 点击 Save Script → 关闭程序 → 重新打开 → Load Script
- **预期**: 加载的脚本包含 10 个动作，类型和参数与录制一致
- **验证**: 逐个比对每个 action 的 type、vk_code/x/y 与录制原始数据完全一致
- **通过标准**: 100% 字段匹配

### TC-005: 直接编辑 .sks 文件
- **前置**: 有一个已保存的 .sks 文件
- **操作**: 用记事本打开 .sks，修改 loop_count 从 1 改为 3，保存
- **预期**: Load 后脚本 loop_count 显示 3
- **通过标准**: 修改生效

## 回放模块

### TC-006: 线性回放成功率
- **前置**: 录制了 5 次按键（A、B、C、D、Enter）共 10 个事件
- **操作**: 加载脚本 → Play
- **预期**: 5 个按键按顺序被模拟触发
- **验证**: 使用外部 Keylog 工具或记事本验证输入
- **通过标准**: 成功回放 ≥ 9/10 事件（90%）

### TC-007: 循环回放
- **前置**: 录制了 2 个鼠标点击事件，脚本 loop_count=3
- **操作**: Play 执行脚本
- **预期**: 2 个点击被执行 3 轮，共 6 次点击
- **验证**: 观察 UI 或日志确认循环 3 次
- **通过标准**: 恰好 3 轮

## 图像识别

### TC-008: 模板匹配 + 自动点击
- **前置**: 当前屏幕存在一个已知图标（如 Chrome 图标），已截取模板图片
- **操作**: 创建包含 image_trigger 的脚本，threshold=0.8
- **预期**: 匹配到目标位置并在图标中心左侧+偏移处点击成功
- **验证**: 点击位置与图标中心偏差 ≤ 10px
- **通过标准**: 单次匹配 ≤ 200ms

### TC-009: 超时处理
- **前置**: 模板图片内容在当前屏幕不存在
- **操作**: 执行含 image_trigger（wait_for_match=true, timeout=3000ms）的脚本
- **预期**: 3 秒后返回 false，继续执行下一动作
- **验证**: 日志记录 "Image trigger timeout"
- **通过标准**: 超时时间偏差 ≤ 500ms

## 防检测

### TC-010: 随机行为验证
- **前置**: 录制并保存一个含 5 个点击事件的脚本
- **操作**: 连续回放 10 次，使用外部工具记录每次点击间隔和坐标
- **预期**: 10 次回放的点击间隔不完全相同，坐标每次有 ±5px 偏移
- **验证**: 计算 10 次回放中任意两次的间隔和坐标差异
- **通过标准**: 不存在完全相同的两次回放记录
